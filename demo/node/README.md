# demo/node

This demo packages a minimal Next.js health endpoint as a Node.js Single
Executable Application with [next-sea](https://github.com/bhbs/next-sea).

Node SEA support is planned after Tamaya v1. The v1 runtime contract is a
single Linux executable that listens on `PORT` and exposes `GET /health`.
